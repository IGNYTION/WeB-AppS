
# IGNYTION Starter (Phase 0)

This is a minimal monorepo to get **ignytion.io** online quickly:

- `apps/frontend/` — static Coming Soon (Vercel deploy)
- `apps/admin/` — static Admin Coming Soon (Vercel deploy)
- `apps/api/` — tiny Node/Express API with `/healthz`
- `.github/workflows/` — CI scaffolding for future use

Next phases will replace frontends with Angular apps and wire DB + storage.
